server:
  port: 9090

spring:
  datasource:
    password: 270907
    url: jdbc:postgresql://localhost:5432/java
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

security:
  jwt:
    secret: Java12
